type PlainTextBodyProps = {
    text: string;
    className?: string;
};
/** Plain CMS copy from textarea fields — preserves single line breaks. */
export declare function PlainTextBody({ text, className }: PlainTextBodyProps): import("react").JSX.Element;
export {};
