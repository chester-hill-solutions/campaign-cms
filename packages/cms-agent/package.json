{
  "name": "@campaign/cms-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Semantic agent tools for the campaign CMS: typed read/write tool handlers over the cms-server store, with Zod input schemas and structured, LLM-recoverable errors.",
  "exports": {
    ".": "./src/index.ts",
    "./tanstack": "./src/tanstack.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@campaign/cms-core": "*",
    "@campaign/cms-server": "*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
